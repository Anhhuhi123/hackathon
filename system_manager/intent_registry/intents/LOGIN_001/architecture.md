# Architecture Contract: LOGIN_001
# Generated by: Architecture Layer
# Source: intent.md (LOGIN_001)
# Version: 1.0.0
# Status: LOCKED

---

> **⚠️ CRITICAL**: File này là **nguồn sự thật duy nhất** giữa FE và BE.
> FE và BE không giao tiếp trực tiếp — cả hai chỉ tuân theo contract này.
> Mọi thay đổi phải được Architecture Layer approve và version bump.

---

## API Endpoints

### Base URL
```
/api/v1/auth
```

### 1. Login — Email/Password

```
POST /api/v1/auth/login
```

**Request:**
```json
{
  "identifier": "string (required, email or username)",
  "password": "string (required)",
  "remember_me": "boolean (optional, default: false)"
}
```

**Response 200 OK:**
```json
{
  "access_token": "string (JWT, expires in 15m)",
  "user": {
    "id": "uuid",
    "email": "string",
    "username": "string"
  }
}
```

**Headers set (httpOnly cookie):**
```
Set-Cookie: refresh_token=<token>; HttpOnly; Secure; SameSite=Strict; Path=/api/v1/auth/refresh; Max-Age=604800
```
> Nếu `remember_me: true` → Max-Age=2592000 (30 days)

**Response 401 Unauthorized:**
```json
{
  "error": "INVALID_CREDENTIALS",
  "message": "Thông tin đăng nhập không chính xác."
}
```

**Response 400 Bad Request:**
```json
{
  "error": "VALIDATION_ERROR",
  "fields": {
    "identifier": "Trường này là bắt buộc",
    "password": "Trường này là bắt buộc"
  }
}
```

**Response 429 Too Many Requests:**
```json
{
  "error": "RATE_LIMIT_EXCEEDED",
  "message": "Quá nhiều lần thử. Vui lòng thử lại sau.",
  "retry_after_seconds": 900
}
```

---

## Authentication Strategy

| Mechanism | Detail |
|---|---|
| Access Token | JWT, RS256, expires 15 minutes |
| Refresh Token | Opaque token lưu trong DB, httpOnly cookie |
| Token Signing | RS256 asymmetric key pair |
| Password Hashing | bcrypt, cost factor 12 |
| Rate Limiting | 5 lần thử sai mỗi 15 phút trên cùng 1 identifier/IP |

---

## Database Decisions

### Table: `users`
```sql
id          UUID PRIMARY KEY DEFAULT gen_random_uuid()
email       VARCHAR(255) UNIQUE NOT NULL
username    VARCHAR(255) UNIQUE NOT NULL
password_hash VARCHAR(255) NOT NULL
created_at  TIMESTAMPTZ DEFAULT NOW()
updated_at  TIMESTAMPTZ DEFAULT NOW()
```

### Table: `refresh_tokens`
```sql
id          UUID PRIMARY KEY
user_id     UUID REFERENCES users(id) ON DELETE CASCADE
token_hash  VARCHAR(255) UNIQUE NOT NULL
expires_at  TIMESTAMPTZ NOT NULL
created_at  TIMESTAMPTZ DEFAULT NOW()
revoked_at  TIMESTAMPTZ  -- NULL = active
```

---

## Technical Constraints

1. Endpoint Login phải thực hiện validate rate limit trước khi check DB để chống Brute-force.
2. Không phân biệt lỗi giữa sai identifier hay sai password để tránh Account Enumeration. Trả về chung `INVALID_CREDENTIALS`.
3. Chỉ lưu refresh token qua httpOnly cookies.
